[2026-02-28 14:31-14:36 GMT+8] WeCom callback re-diagnosis (main session)

- Read required context files: `SOUL.md`, `USER.md`; `MEMORY.md` missing; `memory/2026-02-27.md` missing.
- User requested another troubleshooting pass and suspected cloud-drive/OneDrive probability is low.
- Live checks:
  - OpenClaw gateway on `127.0.0.1:18789` is listening.
  - WeCom gateway on `0.0.0.0:28789` is healthy (`GET /healthz -> ok`).
  - `wecom_gateway.py doctor` returns `run_ready=True`, `send_ready=True`.
- Found tunnel issue:
  - Historical quick-tunnel hostnames are no longer resolvable.
  - Restarted `cloudflared`; obtained new quick tunnel URL:
    - `https://leather-projectors-clause-discovery.trycloudflare.com`
  - Tunnel health verified:
    - `https://leather-projectors-clause-discovery.trycloudflare.com/healthz -> ok`
- Found second active blocker:
  - `wecom_gateway.py send` now fails with:
    - `60020 not allow to access from your ip`
    - error-reported source IP: `103.151.173.100`
  - This implies WeCom trusted IP allowlist needs update again (or widened strategy), otherwise proactive send and async auto-reply will fail.

