# 2026-02-24

- 21:02+ (GMT+8) user requested fallback to previous Enterprise WeCom route.
- Restored `wecom_gateway.py` on `0.0.0.0:28789`; local health check passed: `GET http://127.0.0.1:28789/healthz -> 200 ok`.
- Kept callback tunnel on Cloudflare quick tunnel with `cloudflared` target `http://127.0.0.1:28789`.
- Started a fresh tunnel instance and obtained URL: `https://deputy-gold-wide-leslie.trycloudflare.com` (callback path `/wecom/agent`).
- Stopped personal WeChat notification bridge processes (`wechat_notify_bridge.py`) to avoid dual-route conflicts.
- Patched `wechat_embed/wecom_gateway.py` so `--env-file` is loaded before argparse defaults (port/host now follow env file during `doctor`/`run` defaults).
- Updated `wechat_embed/start_wecom.ps1` default port from `18789` to `28789` to avoid clashing with OpenClaw gateway on `18789`.
- 22:00+ user asked which Enterprise WeCom version to return to and whether backup exists.
- Checked local install version:
  - Registry uninstall entry shows `企业微信 5.0.6.6018`.
  - EXE version at `C:\Program Files (x86)\WXWork\WXWork.exe` is `5.0.6.6018`.
- Workspace search found no old WeCom installer/package backup (`.exe/.msi/.zip` for WeCom/WXWork) in current workspace.
- 22:05+ user clarified they wanted the OpenClaw-WeChat integration project itself reverted to previous Enterprise WeCom version.
- Confirmed project folder: `E:\Desktop\openclaw\.openclaw-workspace\wechat_embed`.
- Reverted project defaults to WeCom mode:
  - Rewrote `wechat_embed/README.md` to WeCom-first workflow.
  - Changed `wechat_embed/start.ps1` to call `start_wecom.ps1` (default `28789`).
  - Added `wechat_embed/start_wechat_personal_legacy.ps1` to preserve old personal WeChat startup script.
  - Updated `wechat_embed/wecom_gateway.py` argparse fallback port from `18789` to `28789` for both `run` and `doctor`.
- Verification after revert:
  - `wecom_gateway.py --env-file .env.wecom doctor` returns callback/health on `28789`.
  - Local health `http://127.0.0.1:28789/healthz` is `ok`.
  - Tunnel health `https://deputy-gold-wide-leslie.trycloudflare.com/healthz` is `ok`.
- 22:15+ user requested auto-start with OpenClaw on boot/login.
- Implemented auto-start companion scripts:
  - `wechat_embed/start_wecom_with_openclaw.ps1`:
    - waits for OpenClaw health on `18789`;
    - ensures `wecom_gateway.py` on `28789` is running;
    - ensures `cloudflared --url http://127.0.0.1:28789` is running;
    - avoids duplicate launches by process pattern checks.
  - `wechat_embed/install_wecom_autostart.ps1`:
    - tries to install `schtasks` (`OpenClaw WeCom Bridge`) on logon;
    - if task creation is denied, falls back to Startup folder command file.
  - `wechat_embed/uninstall_wecom_autostart.ps1`:
    - removes both scheduled task and Startup fallback file.
- Current machine result:
  - `schtasks` creation denied by policy/permission;
  - fallback installed: `C:\Users\jingk\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\OpenClaw-WeCom-Bridge.cmd`.
- Health checks after setup:
  - OpenClaw `http://127.0.0.1:18789/healthz` = `200`;
  - WeCom gateway `http://127.0.0.1:28789/healthz` = `200`.
